namespace Atlas.Presentation.Home
{
    // 表示内容は固定のためPush時に渡す値は無いが、PageLifetimeScope<TViewDto>を常に使う
    // 統一ルール(ForfeitConfirmViewDtoと同様)に合わせて型だけ用意している。
    public sealed class MatchmakingViewDto
    {
    }
}
