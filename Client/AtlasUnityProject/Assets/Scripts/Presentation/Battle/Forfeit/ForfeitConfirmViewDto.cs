namespace Atlas.Presentation.Battle
{
    // 表示内容は固定のためPush時に渡す値は無いが、PageLifetimeScope<TViewDto>を常に使う
    // 統一ルール(HomeViewDtoと同様)に合わせて型だけ用意している。
    public sealed class ForfeitConfirmViewDto
    {
    }
}
